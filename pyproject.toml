[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nfl-warehouse"
version = "0.1.0"
description = "Local Kalshi NFL market extraction primitives"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
kalshi-markets = "kalshi_ingestion.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
